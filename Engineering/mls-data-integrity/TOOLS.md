# TOOLS.md - Workspace Tool Notes

This file is intentionally small. Shared tool behavior comes from OpenClaw skills and runtime instructions; MLS-specific operational behavior comes from the core memory tree.

## Preferred Evidence Sources

- Repository code and migrations for implementation truth
- Supabase / database state for data integrity checks
- Google Cloud / worker logs for runtime behavior
- MLS Grid Analytics for request-count and rate-limit monitoring
- Domain runbooks under `domains/**/runbook.md` for health evaluation

## Local Notes

Add environment-specific details here only when they are useful and safe to store in workspace text. Do not duplicate credentials or immutable bootstrap policy here.
